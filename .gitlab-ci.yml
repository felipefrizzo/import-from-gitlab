image:
  name: felipefrizzo/packer:latest
  entrypoint: [""]

stages:
  - build

build:
  stage: build
  script:
    - ls -lha
    - packer --version
    - packer init mongodb.pkr.hcl
    - ls -lha
    - /usr/bin/packer validate mongodb.pkr.hcl
